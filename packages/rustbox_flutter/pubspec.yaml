name: rustbox_flutter
description: Flutter bindings for the RustBox proxy runtime.
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter_rust_bridge: 2.13.0-beta.5
  flutter_rust_bridge_hooks: 2.13.0-beta.5

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
